# revivo — progress log

Living document. Update after each shippable chunk. Keep dates absolute.

## TL;DR

**Current stage: Stage 2 complete + hardened (code), proven on real data.** Stage 0 (marketing site), Stage 1 (three-variant template), and all of Stage 2 are built: the mockup generator (manual-brief + **places mode**: Google Places + Instagram-light → SalonBrief), the Supabase **`mockups` table**, and the **`mock.revivo.nl` SSR app** that renders a salon at a shareable `/{slug}` URL through the customer-template variants. The full loop is validated against **real** infra + a **real** prospect: a live Google Place (Utrecht Hairstyle) → Claude (OpenRouter) → grounded SiteConfig → Supabase → rendered in the mock app. A 5-lens adversarial audit of that first real mockup drove a **Stage 2.5 hardening pass** (the generator no longer fabricates KvK/BTW/email/booking-URLs; real lat/lng/rating ride through as structured grounding), and the mock app is **deployed** (Vercel, `revivo-mockups.vercel.app/{slug}`). Most recently, finding the mockup factually wrong about the salon's own business drove **Stage 2.6 — real-data sourcing**: a deterministic Treatwell scraper (`window.__state__` → typed `ListingFacts`, no LLM) plus `applyListingFacts`, which overwrites the config's factual fields (services/prices/hours/team/reputation/real reviews/real photos/booking) with the scraped truth while the LLM authors only voice. Verified end-to-end on Utrecht Hairstyle (33/33 ground-truth scraper checks; real LLM run grounded correctly). **Phase 5 + 5.5 done:** all three variants render `team` + `reputation` + real reviews + real photos in their own DNA, and the **3-template acceptance test passes** (atelier/studio/neon all correct on the Utrecht real-data config — 0 picsum, 0 fabrication). Next: the **Qwen vision pass as a *measurement*** of residual scraper mistakes across ~5 salons (Phase 6/6.5), which decides whether the vision check ever becomes a runtime `--push` gate.

## Stages

The full design + staged plan lives at `~/.claude/plans/i-want-to-build-peaceful-pumpkin.md`. Summary status:

- [x] **Stage 0** — revivo.nl marketing site + monorepo scaffold (commit `e7d5f91`)
- [x] **Stage 1** — customer-template with three variants (commits `f4ab7b8`, `87588ec`)
- [x] **Stage 2** — mockup generator (manual + places mode) + Supabase `mockups` table + `mock.revivo.nl/{slug}` SSR app. Code complete; live Places/Supabase paths need keys.
- [ ] **Stage 3** — lead workspace admin (Next.js + Supabase)
- [ ] **Stage 4** — sourcing pipeline (Google Places + KvK + qualification)
- [ ] **Stage 5** — build & deploy automation (TransIP + Vercel API)

## What's done in detail

### Stage 0 — revivo.nl marketing site (2026-05-25)

- pnpm workspace monorepo (`apps/*`, `packages/*`) with Turbo-friendly layout
- `apps/marketing/` — Astro 5 + Tailwind v4 single-page site
  - Sections: Hero · Wat je krijgt · Investering · Het proces · Contact · Footer
  - Brand palette + Cormorant Garamond serif headlines + italic gold accents
  - Faithful translation of `revivo-proposal.pdf` (committed for reference)
- Verified: production build passes, both desktop + mobile screenshots match PDF
- Cal.com URL placeholder hard-coded in `apps/marketing/src/components/Contact.astro:3`

### Stage 1 — Customer site template (2026-05-25)

- `apps/customer-template/` — Astro app, JSON-config-driven
- `SiteConfig` TypeScript type + Zod schema in `src/types/site-config.ts` — contract the mockup generator must produce
- `src/data/load-config.ts` loads from `REVIVO_CONFIG` env var (path to JSON)
- **Three variants**, same data, different design DNAs:
  - **Atelier** — warm editorial: Fraunces serif + DM Sans, asymmetric grid, drop caps
  - **Studio** — brutalist minimal: Bricolage Grotesque + IBM Plex Mono, hairlines, monumental headlines
  - **Neon** — bold contemporary: Unbounded + Hanken Grotesk, color-blocked sections, marquee
- `src/pages/index.astro` routes on `config.layout`
- Three example configs in `examples/`:
  - `lume-atelier.json` (Amsterdam-Zuid hair salon)
  - `mast-studio.json` (Rotterdam concept studio)
  - `spark-hair.json` (Rotterdam electric-blue concept salon)
- Verified: all three render correctly on desktop + mobile, both builds pass

### Stage 2 — Mockup generator MVP (2026-05-26)

- `packages/shared` — extracted the `SiteConfig` Zod schema here (the contract) so both the template (consumer) and generator (producer) share it. 29 imports updated.
- `packages/llm` — model-agnostic LLM pipeline:
  - `client.ts` — `LLMClient` interface + OpenAI-compatible adapter (covers OpenRouter + OpenAI). Provider/model/base-URL all from env; switching is a config change.
  - `prompts/mockup-system.ts` — the generator system prompt (variant-pick rubric, palette/copy/pricing guidance, schema).
  - `mockup-generator.ts` — brief → SiteConfig, Zod-validated, 1 retry on schema miss; image URLs rewritten to deterministic picsum BEFORE validation.
  - `dry-run.ts` — deterministic stub (no API call) for plumbing tests.
  - `bin/gen-mockup.ts` — CLI. `pnpm gen-mockup --name ... --city ... --vibe ...` (or `--brief file.json`, `--dry-run`).
- Verified live via OpenRouter (`anthropic/claude-sonnet-4.5`): "Bloom Beauty" (luxe beauty) → **atelier** with sage palette; "VOLT" (brutale Gen-Z) → **neon** with hot magenta + "Haar dat schreeuwt". Both rendered + screenshot-checked. ~€0.04/mockup, 1 attempt after the image-injection fix.
- Generated configs land in `apps/customer-template/examples/generated/` (gitignored — reproducible via CLI).

### Stage 2 — places mode + Supabase sink + mock app (2026-06-03)

- **`SalonBrief` + `slugify` moved to `@revivo/shared`** (was in `packages/llm`). Both the brief *producer* (`@revivo/sourcing`) and *consumer* (`@revivo/llm`) now share the input contract without coupling, mirroring how `SiteConfig` already lives there.
- **`packages/sourcing`** — places mode: Google **Places API (New)** client (`getPlaceDetails` / `searchSalonByText` / photo URLs, header auth + field masks, `fetch` only), **Instagram-light** (handle normalize + pasted bio/captions + env-gated provider seam — no scraping), and `placeToBrief()` which passes through *real* facts (name, address, phone, **real opening hours**, rating, reviews, IG bio) and never invents character. `assembleBriefFromPlaces()` / `assembleBriefFromFixture()` are the entry points; `FIXTURE_PLACE` powers offline/dry-run + the e2e test.
- **`packages/db`** — service-role Supabase client (server-side only) + `mockups` helpers (`upsertMockupBySlug` / `getMockupBySlug`, `config_json` typed as `SiteConfig`). `supabase/migrations/20260603093000_mockups.sql`: the table + RLS (service-role-only) + `updated_at` trigger.
- **`apps/mockups`** — Astro **SSR** (`@astrojs/node`) at `mock.revivo.nl/{slug}`. `[slug].astro` looks up the config (Supabase, or local example JSON when unset) and **rewrites to a per-variant render page** that reuses the customer-template variant components via a `~`→`../customer-template/src` alias. Per-variant pages are deliberate: under SSR a single page importing all three variant Layouts merges their Tailwind globals into one sheet. Each variant CSS now self-declares `@source` so Tailwind generates its utilities when built from the mock app.
- **CLI** (`pnpm gen-mockup`) gained `--place-id` / `--query` / `--fixture-place`, `--ig` / `--ig-bio` / `--ig-captions`, and `--push` (upsert into Supabase). `--dry-run` still costs nothing.
- **Verified**: all packages typecheck; customer-template + marketing + mockups build; a **real** places-mode run (`--fixture-place`, fixture Place → Claude via OpenRouter, 1 attempt, ~€0.04) produced a SiteConfig grounded in the fixture's real hours/address/IG and review voice; the mock app served all three variants (desktop + mobile) + 404, screenshot-checked. Two SSR-specific bugs were found and fixed during verification: (1) all-variant CSS merging under SSR → fixed with the rewrite-to-per-variant-page pattern; (2) the mock app's `NotFound`/`index` chrome used global `body{}` styles, which Astro leaves *unscoped*, leaking `display:grid` into every page that imports them and breaking the neon hero's layout → fixed by scoping those styles to a wrapper.
- **Now live (2026-06-03)**: Supabase is provisioned (EU/Ireland, ref `zbdmmdzqwaynijspbacu`) and the `mockups` table is deployed. Migrations ship via a **GitHub Action** (`.github/workflows/deploy-supabase-migrations.yml`) that runs `supabase db push` on push to `main` — the native Supabase↔GitHub integration only does PR preview branches, not prod deploys, so the Action is what actually deploys schema (needs repo secrets `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD`). Full loop verified against real infra: `--fixture-place --push` → row in Supabase → mock app serves `/kapsalon-mira` straight from the DB (proven by hiding the local file). `GOOGLE_PLACES_API_KEY` is set + validated (live text search returns real NL salons).

### Stage 2.5 — generator hardening (2026-06-03)

The first real `--place-id` run (Utrecht Hairstyle, `ChIJ2eOq…`) surfaced that the **system prompt was *instructing* the generator to fabricate verifiable facts** — and a 5-lens, adversarially-verified audit (21 confirmed findings) confirmed the impact: a fake KvK + BTW in the footer, a dead Treatwell booking CTA, a guessed/404 Instagram link, an invented email, and the Google review count laundered into a "230+ tevreden klanten" stat. None were model misbehaviour — prompt lines 52–54 literally said "verzin plausibele …" and `SiteConfig.legal.kvk` was a *required* string.

Fixed (the mockup is now safe to WhatsApp to a real prospect):
- **Prompt** (`mockup-system.ts`) — replaced every "verzin" clause with hard prohibitions: never fabricate KvK/BTW (omit), never invent email/IG/WhatsApp (omit; WhatsApp only for a real 06/+316 mobile), `booking.provider` defaults to `custom` with no invented third-party URL, `about.stats` only from briefed numbers (never convert review count → klanten; no invented jaren/stylisten), paraphrase testimonials with a neutral anonymous byline, never invent lat/lng. Added a layout anti-default (rating < 4.5 / mixed reviews → `atelier`, not `studio`) and an accent-contrast (≥3:1 vs ink **and** surface) + reserve-metallics rule.
- **Schema** — `legal` is now `.default({})` and `legal.kvk` optional, so the model can omit legal entirely without a wasted retry (was a required string forcing a fake).
- **Pipeline** (`placeToBrief`) — carries the **real** `lat`/`lng`/`rating`/`reviewCount` through as structured `SalonBrief` fields (`briefToMessage` surfaces them as authoritative grounding), and emits "Contact-aanwijzingen" notes (vast nummer → geen WhatsApp; geen website → verzin geen e-mail).
- **Variants** — all three footers suppress the KvK/BTW line when absent and strip a leading `@` before building the Instagram URL (fixes the `instagram.com/@handle` 404); all three booking sections fall back to `tel:` (then `#`) instead of a dead anchor; studio guards the `provider` label / neon the booking copy against `custom`.
- **Verified end-to-end**: regenerated Utrecht Hairstyle → valid config in **1 attempt**, now `layout: atelier`, `legal: {}`, `booking.provider: custom`, **real coordinates** `52.0936634, 5.1192292`, no email/IG/whatsapp, neutral testimonial bylines. Rendered page contains zero fabrications (KvK/BTW/treatwell/instagram.com/"230"/"tevreden klanten" all absent) and the booking CTA resolves to `tel:+31302685951`. Also fixed an SSR bug found en route: the mock app 500'd in dev because Astro's node adapter externalized the raw-TS `@revivo/*` packages → `ssr.noExternal: [/^@revivo\//]`.

### Deployed — mock app live on Vercel (2026-06-03)

The mock app is **live and publicly shareable**: **https://revivo-mockups.vercel.app/{slug}** (e.g. `/utrecht-hairstyle`). The full moat loop now runs on the public internet: live Google Place → Claude (OpenRouter) → Supabase → Vercel SSR render.

- Adapter swapped `@astrojs/node` → **`@astrojs/vercel@8`** (Astro-5 line; `@astrojs/node` removed). `output: "server"` unchanged; builds to `.vercel/output` (gitignored).
- Vercel project **`berend-66s-projects/revivo-mockups`** (Hobby scope). Deployed via `vercel deploy --prebuilt --prod` (build locally, upload — avoids configuring Vercel's pnpm-monorepo remote build).
- Runtime env set in Vercel **Production**: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (service-role, server-side only — read in SSR, never client-shipped). Verified the live page serves the Supabase row, not the local fallback.
- **Deployment Protection (Vercel Authentication) was ON by default** → the `*.vercel.app` URLs returned 401 behind Vercel SSO. Disabled via the API (`PATCH /v9/projects/{id}` `ssoProtection: null`) so the link is publicly openable.
- Caveat: env vars are set for **Production only** (the interactive preview-branch prompt skipped them) — fine for `--prod` deploys; set Preview too if GitHub-connected preview deploys are ever wired.

**Next actions: (1) attach `mock.revivo.nl` once `revivo.nl` is registered (it's still on the manual-TODO) — a custom domain matters before sending links to real prospects; a `*.vercel.app` link reads as spammy. (2) Real-photo passthrough (the moat) — resolve `place.photoNames` server-side + re-host (key-free URLs) and inject in `normalizeImagesInPlace`, so deployed mockups show the salon's OWN photos instead of picsum.**

### Stage 2.6 — real-data sourcing (Treatwell) + facts passthrough (2026-06-03)

Berend checked the first real Utrecht Hairstyle mockup against the salon's actual Treatwell page and found it wrong on nearly everything the LLM had invented: prices (balayage €165 vs real *from* €70), services (invented Keratine/Olaplex; missed the salon's **threading** specialty), **no team**, reputation hidden (we showed nothing; reality 4,7★/2.907), wrong hours (Google's, not the salon's), generic paraphrased reviews, and picsum photos. Root cause: Google Places is too thin a source, so the model filled the gaps. The fix is to source the salon's **real** facts and stop the model from inventing them.

Built (Phases 1–3 of the real-data plan; **facts deterministic, voice LLM**):
- **`@revivo/sourcing/treatwell.ts`** — a pure fetch+parse scraper (no LLM, no new dep). A Treatwell salon page is server-rendered and embeds a `window.__state__` JSON blob (+ schema.org JSON-LD), so extraction is **deterministic JSON parsing**, not an LLM call. `fetchTreatwellListing` → `RawListing`; `treatwellListingToFacts` → a validated `ListingFacts` (services+prices+durations, hours, team incl. per-stylist rating/sentiment, reputation, curated reviews, photos, phone, geo); `listingFactsToBrief` builds the brief. JSON-LD is a redundant fallback for the scalar fields.
- **`@revivo/shared`** — new `ListingFacts` schema (reuses SiteConfig's own sub-schemas); added optional `team` (`TeamMember`) + `reputation` to `SiteConfig` (additive — every existing config/row still validates; variants render-if-present).
- **`@revivo/llm`** — `generateMockup(brief, client?, facts?)`: when facts are present it (a) surfaces them as an authoritative "ECHTE GEGEVENS" grounding block so the model's **voice** (brand/colour/headline/about/captions/layout) fits the real salon, then (b) `applyListingFacts` deterministically **overwrites** the factual fields (services, hours, team, reputation, testimonials=real reviews, contact.phone, booking=real Treatwell URL, location, **photos**) — the model's invented facts are discarded, so drift can't reach the mockup. The prompt tells the model to omit team/reputation/testimonials (filled from facts). Also added `stripStrayNullsInPlace` (a pre-validation pass that drops stray `null`s the model emits for optional fields — preserving the one legitimately-nullable `price`).
- **Photos folded in** — Treatwell's CDN photo URLs carry no API key, so the Google photo-proxy isn't needed for this path. `applyListingFacts` fills hero (≤4) + gallery from the real photos, **sized dynamically** to however many exist; picsum survives only when there are none.
- **CLI** — `--treatwell <url|slug>` (standalone, or combined with `--place-id` to borrow Google's postcode + coords). New `source: "listing"` (migration `20260603120000_mockups_source_listing.sql` widens the CHECK; `MockupSource` union updated).
- **Verified**: a deterministic scraper test hit **33/33** ground-truth assertions for Utrecht Hairstyle (real prices, 7 categories/26 services incl. threading, correct hours, 5 team members with Carmen's low-count rating suppressed, 2★ review dropped, 15 photos, 4,7★/2.907, geo). A `--dry-run --treatwell` offline pass produced a valid config with every fact correct. A **real** LLM run (`anthropic/claude-sonnet-4.5`, 3.2k in / 2.0k out) produced grounded voice (tagline "20 jaar internationale expertise", about prose weaving Nael's Dubai/Libanon history + threading + "voor iedereen", `about.stats` correctly absent) with all factual fields overwritten by the truth. All of `@revivo/shared|sourcing|llm|db` typecheck clean.
- **Renders now** (existing sections): services/prices, testimonials (real reviews), hours, hero+gallery (real photos), booking (real URL), contact, location. **Pending render**: `team` + `reputation` need a new section in each of the 3 variants (Phase 5).
- **Known gap**: Treatwell has no postcode, so treatwell-only mode leaves `location.postcode` to the model (plausible but a guess); combine with `--place-id` for the real one.

### Stage 2.6 (Phase 5) — render team + reputation in all three variants (2026-06-04)

Each variant now renders `config.team` (a stylist grid, render-if-present) + weaves `config.reputation` into its Hero — each matching its own DNA, sourced not invented:
- **atelier** — `sections/Team.astro`: editorial portrait grid (Fraunces names, mono role labels, hairline dividers, desaturated headshots, Fraunces-initial fallback); reputation as a mono `eyebrow` in the Hero CTA row.
- **studio** — `sections/Team.astro`: numbered brutalist contact-sheet ("The chairs.", `N°NN` framed grayscale headshots, `label-mono` captions); reputation as a quiet `text-ink` line in the Hero meta rail. The hand-numbered `NN / 07` rails are made team-aware (StudioDetails→`06`, Booking→`07` when team present; revert to `05`/`06` when absent) so numbering stays correct for non-Treatwell salons.
- **neon** — `sections/Team.astro`: rotated photo-wall cards ("Wie je knipt.", un-rotate on hover, primary-colour rating tags); reputation as an oversized primary-colour stat strip under the Hero CTA.
- All gated `{config.team && config.team.length > 0 && …}` in each Layout (reputation guarded inline in Hero), so absence renders nothing — every existing config still validates/renders.
- **Verified live** (neon, Utrecht config on the dev server): Team renders all 5 stylists with photos, specialties (Professioneel/Vakkundig), per-stylist ratings + counts, and the reputation line "4,7★ op Treatwell · N reviews".

### Stage 2.6 (Phase 5.5) — prompt tweaks, data-fix round, + 3-template acceptance test PASSED (2026-06-04)

Two prompt tweaks (`prompts/mockup-system.ts`):
- **Cliché ban widened** from the single literal "uw haar, onze passie" to the whole "[zelfstandig naamwoord], onze passie" slogan template (+ a blocklist: "kwaliteit staat voorop", "in goede handen", "oog voor detail", …). Validated: regenerating Utrecht flipped the headline from "Jouw look, onze passie" → "Waar Utrechtse gezelligheid internationale klasse ontmoet".
- **Family/established salons steer to atelier even at high rating.** A high rating no longer implies studio/neon; a multi-generation/neighbourhood salon (high review count built over years, broad menu, loyal mixed clientele) → atelier. Validated: Utrecht Hairstyle (20yr, ~2.9k reviews, threading + kids + men menu) now picks **atelier** instead of neon.

A 6-agent adversarial audit of the three built variants (one auditor + one skeptic per variant) caught real gaps, all fixed this round:
- **`about.portrait` picsum leak** — `applyListingFacts` filled hero + gallery from real photos but never `about.portrait`, so the model's picsum placeholder survived and rendered (visible in atelier). Fix: set `about.portrait` to a real Treatwell photo (one beyond the hero set) when photos exist → **0 picsum anywhere** in the config.
- **studio + neon rendered NO reviews** — Phase 5 added Team to all three, but a Testimonials section only existed in atelier. Built `sections/Testimonials.astro` for studio (brutalist bordered quote cards, "Word of mouth.", `06/08 — REVIEWS` rail) and neon (rotated primary-colour quote cards, "De reviews."), wired into both Layouts (render-if-present). Real reviews now show in all three.
- **studio numbering refactor** — the hand-numbered `NN / 07` rails (already team-conditional) couldn't absorb a 2nd optional section. Replaced the per-section ternaries with a computed `sections/_rail.ts` (`studioRail(config)` → number per section + total, derived from which optionals are present). Correct for every combo now: with team + reviews → `02/08 … 08/08`.
- **studio phone** now surfaced in Booking (was displayed nowhere).
- **review author-dedup** (`treatwell.ts`) — one review per NAMED author (anonymous bylines never collapsed), so two reviews by the same "Rik" no longer render adjacent on the opener.

**Acceptance test PASSED** — generated Utrecht with real Treatwell facts, forced `layout` to each variant, built static, and audited the HTML: all three render team (5, Carmen correctly rating-less), reputation (4,7 / 2.909 / Treatwell), the 3 real reviews, real Treatwell photos (hero + gallery + portrait), correct prices (€25 men's cut, €5 threading) and hours (Mon 11:30 / Thu 20:00) — **0 picsum, 0 fabrication, 0 broken markup**. Served at localhost:4411 (atelier) / 4412 (studio) / 4413 (neon).

> **Audit methodology note:** the audit also flagged "Nael 873 not 871" and "Rik instead of Lilian/Tim" — both **false positives from stale ground truth**. The live Treatwell data drifted between the pre-regen snapshot and the fresh scrape (reputation 2907→2909, Nael 871→873; different recent reviews surfaced). This actually confirms the scraper pulls *live* data faithfully. Lesson: snapshot the audit's ground-truth from the **same** scrape the config was built from.

**Next: Phase 6/6.5 — the Qwen vision pass as a *measurement* of residual scraper mistakes across ~5 salons (decides whether the vision check ever becomes a runtime `--push` gate). Confirm the exact OpenRouter slug for the vision model before the first call.**

## Manually pending (Berend's TODO)

These steps need a human — Claude can't do them.

| What | How | Why |
|---|---|---|
| Register **revivo.nl** | TransIP (~€10/yr) | Domain needed before deploy |
| Set up **hallo@revivo.nl** mailbox | TransIP mailbox or Google Workspace | Contact CTA needs to land somewhere |
| Set up **cal.com/revivo/kennismaking** | Free Cal.com account | Hero + Contact CTAs link here |
| Deploy `apps/marketing` to Vercel | `cd apps/marketing && npx vercel --prod` or connect repo via Vercel UI | Go live |
| Get **Anthropic API key** | console.anthropic.com | Optional — generator runs on the existing OpenRouter key today; native Anthropic later for cheaper + prompt caching |
| Get **TransIP API key** | TransIP control panel | Required for Stage 5 automated domain reg |

✅ Done since: **Google Places API key** (set + validated, live), **Supabase project + `mockups` table** (deployed via the migrations GitHub Action), **mock app deployed to Vercel** (publicly live at `revivo-mockups.vercel.app/{slug}`; custom domain `mock.revivo.nl` still awaits the `revivo.nl` registration above).

## Open decisions / unknowns

- **Booking widget UX per provider** — Treatwell vs Salonized vs Booksy each have their own embed quirks. Defer until first 1–2 real customers tell us which they use.
- **Photography fallback** — what to do when a salon's Insta photos are unusable (low res, all vertical, all selfies). Layout-variant choice mitigates somewhat; long-term may need a paid "shoot day" upsell.
- **Domain ownership at delivery** — plan says register in customer's name; need to confirm TransIP API supports that path or whether we have to use a transfer flow.
- **Marketplace directories as the Stage-4 lead source (high-potential avenue).** Treatwell / Fresha (and Salonkee etc.) run browsable partner *marketplaces* — an "Airbnb for salons" directory by city + treatment type (the Treatwell state blob even exposes a `venueListingUri`, e.g. `treatwell.nl/salons/bij-kapper/in-lange-jansstraat-nl/`). Enumerating these could replace/augment Google Places as the prospect source — and crucially **each marketplace entry IS the listing URL the scraper already consumes**, so lead-sourcing and mockup-data collapse into ONE step (no separate Places lookup; richer data per lead than Google gives). Qualification nuance to validate: a salon on Treatwell has *proven demand* (real reviews) but typically **no owned website** (a marketplace listing ≠ a branded site) → likely a **strong** revivo fit; the pool does skew toward salons already paying for a booking platform (some digital maturity/budget — could cut either way). Same public-page-read / TOS posture as the scraper; crawl politely + rate-limit. Source: a Fresha employee (Berend's contact). Evaluate against Google Places when Stage 4 sourcing is designed.

## Decision log

Append-only. Date, decision, why.

- **2026-05-25 — €1.000 flat one-time + optional €10–15/mo care plan.** Locked in via PDF proposal. Subscription primary was considered but rejected — one-time matches existing proposal and lower commitment for shop owners.
- **2026-05-25 — WhatsApp click-to-send via `wa.me` deep links + IG manual paste + email follow-up via Instantly.** Berend's preferred channels. Postcards rejected. WhatsApp Business API deferred until volume justifies.
- **2026-05-25 — pnpm monorepo + Astro + Next.js + Supabase + Claude.** All "boring" choices to keep one-operator load low. Inngest deferred unless cron + queue table prove insufficient.
- **2026-05-25 — Three layout variants (Atelier / Studio / Neon).** Polar-opposite design DNAs from the same SiteConfig. LLM will pick variant per-salon based on extracted brand vibe in Stage 2.
- **2026-05-25 — Customer site hosting model: revivo hosts on its own Vercel account, one project per customer, custom domain attached at go-live.** Care plan (€10–15/mo) is the upsell that makes this sustainable.
- **2026-05-26 — LLM is model-agnostic, defaulting to OpenRouter with Claude models.** Berend has an OpenRouter key today (and OpenAI); Anthropic gettable. OpenRouter lets us use Claude (`anthropic/claude-sonnet-4.5`) without waiting. Generator depends only on an `LLMClient` interface; provider is an env change. Reason to keep it swappable: OpenRouter has a markup, so native Anthropic (cheaper + prompt caching) stays a one-edit migration. This supersedes the plan's "Anthropic SDK direct" default for now.
- **2026-05-26 — `SiteConfig` contract relocated to `packages/shared`.** Both the template and the generator depend on it; it no longer lives inside an app.
- **2026-05-26 — Stage 2 built CLI-first / file-based** (chosen over full Supabase flow) to validate the moat cheaply before investing in DB + deploy infra. Generated configs are local files rendered by the existing template.
- **2026-05-26 — Generator rewrites image URLs to placeholders before Zod validation**, not after — a malformed model URL must never trigger a costly retry over a field we discard anyway.
- **2026-06-03 — `SalonBrief` + `slugify` moved to `@revivo/shared`.** It's the *input* contract (the SiteConfig of inputs); co-locating it with `SiteConfig` lets `@revivo/sourcing` (producer) and `@revivo/llm` (consumer) share it without sourcing depending on llm.
- **2026-06-03 — Places mode goes through the existing `SalonBrief` → `generateMockup` path**, not a separate generator. `placeToBrief` maps a Google Place (+ Instagram-light) into a brief; the LLM step is unchanged. Real facts (hours/address/phone/reviews/IG bio) ride along in `brief.notes` so the mockup mirrors reality — credibility is what makes the WhatsApp opener land.
- **2026-06-03 — Instagram-light is intentionally minimal**: handle + human-pasted bio/captions + an env-gated `InstagramProvider` seam (no adapter yet). No scraping — TOS/ban risk; matches the plan's "manual, no scraper" stance.
- **2026-06-03 — `mockups` RLS locked to service-role only.** Both the generator (write) and the mock app (read, in SSR) use the service-role key server-side; no anon policy means mockups can't be enumerated via the public API — the slug is the capability. Revisit if a public listing is ever needed.
- **2026-06-03 — Mock app reuses the customer-template variants (one source) via a `~` alias**, rather than forking them. To keep each mockup page shipping only its variant's CSS under SSR, `[slug].astro` rewrites to a per-variant render page (statically importing all three would merge their Tailwind globals). Each variant CSS self-declares `@source` so it's portable across apps.
- **2026-06-03 — Mock app adapter is `@astrojs/node` (standalone)** for portable local verification; swapping to `@astrojs/vercel` for edge deploy is a one-line change. Edge caching is via `Cache-Control: s-maxage`.
- **2026-06-03 — Generator never fabricates verifiable/clickable facts (Stage 2.5).** KvK, BTW, email, Instagram handle, WhatsApp, and third-party booking URLs are omitted unless they come from the brief — a mockup is an opener the real owner inspects, so a fake KvK or a dead "Boek je afspraak" is a credibility/conversion killer. Real grounding (lat/lng, rating, review count) rides as structured `SalonBrief` fields, not just prose, so the model copies facts instead of inventing them. This makes the package CLAUDE.md "pass real facts, never invent character" rule binding on the prompt too, not only the pipeline. Found via a 5-lens adversarial audit of the first real `--place-id` mockup.
- **2026-06-03 — Layout selection is rating-aware; `studio` is not a default.** `studio`'s brutalist high-fashion DNA overstated a mid-market kapper (4.3★, mixed reviews); the prompt now steers rating < 4.5 / value-signal salons to `atelier` and reserves `studio` for briefs that explicitly signal premium-minimal positioning.
- **2026-06-03 — Real photos remain a deferred fast-follow.** `place.photoNames` (10 real photos for the test salon) are still discarded for picsum — wiring them needs server-side resolution + re-hosting because `placePhotoMediaUrl` embeds the API key (can't go in a public config). Tracked as the top post-deploy task since "their own photos" is the moat.
- **2026-06-03 — Mock app deploys to Vercel via `--prebuilt`, not remote build.** Build locally (`astro build` → `.vercel/output`) and upload with `vercel deploy --prebuilt`, rather than letting Vercel install + build remotely. Reason: configuring Vercel's remote build for a pnpm workspace subdir (root dir + workspace install) is fiddly; prebuilt sidesteps it entirely and is deterministic. If we later connect the repo for git-push auto-deploys, revisit (set Root Directory = `apps/mockups`, install at workspace root, and add Preview env vars).
- **2026-06-03 — Vercel Deployment Protection disabled for the mock project.** New projects default to Vercel Authentication (SSO) gating every `*.vercel.app` URL behind login — incompatible with the product premise (the URL IS a cold opener the prospect must open without an account). Disabled via API (`ssoProtection: null`). The slug remains the only capability (mockups aren't enumerable; RLS is service-role-only). Re-enable for any internal/admin surface, never for the public mock app.
- **2026-06-03 — Migrations deploy via a GitHub Action, not the native Supabase integration.** The native Supabase↔GitHub integration only powers PR preview branches; it does not apply migrations to production on a plain push. `.github/workflows/deploy-supabase-migrations.yml` runs `supabase db push` on push to `main` (repo secrets `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD`; project ref hardcoded, not secret). Verified end-to-end: push → table created → `--push` row → mock app serves it from the DB.
- **2026-06-03 — Treatwell is the real source of truth; Google Places stays for coords/postcode/extra photos.** A mockup that's confidently wrong about the owner's own menu/team is worse than none (it kills the moat). The salon's Treatwell page is its public truth and scrapes cleanly. Google Places alone is insufficient (no menu/team/prices) — keep it for coordinates, postcode, and as the fallback when a salon has no Treatwell page (hardened omit-don't-invent still applies there).
- **2026-06-03 — Listing extraction is deterministic JSON parsing, NOT an LLM call.** Treatwell salon pages embed a complete `window.__state__` blob; a non-LLM parser gets ~100% of the structured fields (services/prices/durations, hours, team+ratings, reviews, reputation, photos, geo). This is the "facts deterministic, voice LLM" principle taken fully: zero model drift on facts, zero extra cost, and it keeps the scraper in `@revivo/sourcing` (which must never depend on `@revivo/llm`). The planned LLM `extract-listing.ts` is therefore **deferred** as an insurance fallback only — to be built if/when a listing lacks the state blob (the measurement phase will tell us).
- **2026-06-03 — `applyListingFacts` overwrites factual fields AFTER generation; the LLM authors only voice.** The model is given the real facts as authoritative grounding (so its copy/colour/layout fit the salon) and told to omit team/reputation/testimonials; the deterministic passthrough then writes services/hours/team/reputation/reviews/contact/booking/location/photos from the scraped truth. Model-invented facts are discarded — drift cannot reach the mockup. Generated config is re-validated after the passthrough.
- **2026-06-03 — Photos pulled from Treatwell directly (no proxy needed for this path).** Treatwell CDN image URLs (`cdn1.treatwell.net`) carry no API key, so they're public-safe to store in the config — unlike Google `placePhotoMediaUrl` (key in the query string). `applyListingFacts` sizes hero (≤4) + gallery dynamically to the real photo count; picsum is now genuinely last-resort. The Google photo-proxy is still needed only for the Places-only (no-Treatwell) path.
- **2026-06-03 — Reviews are curated to ≥4★ from the listing's REAL reviews, not paraphrased/anonymised.** Selecting the salon's own positive reviews (real author + real text + `source: "Treatwell"`) is what any salon's marketing site does — it's selection, not fabrication, so it doesn't violate the no-invent rule. The earlier "paraphrase + anonymous byline" rule only governs the *places/manual* path, where we don't have verified review provenance.
- **2026-06-04 — Listing extraction is a pluggable per-platform adapter; extend beyond Treatwell on demand (FOLLOW-UP).** `ListingFacts` (the contract) + `applyListingFacts` (the consumer) are platform-agnostic; only the extractor is bespoke. Planned follow-up adapters, each a sibling of `treatwell.ts` emitting `ListingFacts`: **Fresha, Salonized, Salonkee, SalonHub, Aimy, Zenoti** (Treatwell first — it dominates NL hair/beauty). Priority is **coverage-driven**: measure what share of the real prospect list uses each platform before building it. Technique per platform mirrors Treatwell (deterministic embedded-state / JSON-LD parse) where possible; LLM/vision extraction is the fallback for sites with no structured data. A generic schema.org-`LocalBusiness` extractor is a cheap interim covering scalar fields (name/address/geo/rating/hours/photos) for non-Treatwell sites. Tracked in `packages/sourcing/CLAUDE.md` "Not built yet".
- **2026-06-04 — A high rating does NOT imply studio/neon; established family/neighbourhood salons go to atelier.** The first real salon (Utrecht Hairstyle: 20yr, ~2.9k reviews, broad menu incl. threading/kids/men) was being assigned `neon` purely on its strong rating, which misreads its character. The layout prompt now treats a high review count built over years + a broad menu + a loyal mixed clientele as **atelier** signals, and reserves studio/neon for explicitly design-/social-driven salons targeting a young trend audience. Also widened the cliché ban from one literal phrase to the entire "[noun], onze passie" slogan template. Both validated by regeneration (atelier + a concrete, non-cliché headline).
- **2026-06-04 — `applyListingFacts` also fills `about.portrait` from a real photo.** The passthrough wrote hero + gallery but not the editorial portrait, so the model's picsum placeholder survived and rendered on a real mockup. Now the portrait gets a real Treatwell photo (one beyond the hero set, for variety) whenever any exist → no picsum survives when the salon has its own images.
- **2026-06-04 — Reviews are deduped by named author before display.** Treatwell's embedded state can surface two reviews by the same person; "Rik / Rik" adjacent reads like a bug on the opener. The scraper now keeps one review per *named* author (highest-rated; anonymous/generic bylines are never collapsed). Done in `treatwell.ts` so `ListingFacts.reviews` is presentation-clean for any consumer.
- **2026-06-04 — All three variants now render Team + Testimonials; studio numbering is computed, not hand-coded.** Phase 5 left reviews rendering only in atelier; studio + neon got their own `Testimonials.astro` (each in its DNA). Studio's `NN / 07` rail couldn't absorb a 2nd optional section, so it's now derived from config via `studio/sections/_rail.ts` (`studioRail`) — correct for any present/absent combination of Team + Testimonials. The 3-template acceptance test (Utrecht real-data config forced to each layout, built + HTML-audited) passes with 0 picsum / 0 fabrication / 0 broken markup.
- **2026-06-04 — Audit ground-truth must be snapshotted from the SAME scrape the config was built from.** The acceptance audit produced false-positive "fabrication" flags (Nael 871→873, different top reviews) that were actually faithful live-Treatwell drift between two scrapes minutes apart. Live data moving is correct behaviour; the audit was comparing against a stale snapshot. When the vision/measurement phase compares mockup-vs-listing, both must come from one fetch.
